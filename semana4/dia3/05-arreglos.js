/**
 * ARREGLOS
 */

const numeros = [5, -1, 11, 777]
    // imprimer arreglo dada su posicion

console.log(numeros[1]);

// imprimir todos los elementos 
console.log("imprmir todos los numeros del arreglos")
for (let c = 0; c < numeros.length; c++) {
    console.log(numeros[c]);
}
/// imprmir todo el arreglo
console.log(" imprimir todo el arreglo")
console.log(numeros);

/// imprirmir el tamano del arreglo o cantidad que tine
console.log("cantidad de elementos que tiene")

console.log(numeros.length);